import BaseLineIndex from './index'

const children = [
]
const roots = [
  {
    path: '/case/baseline/index',
    name: 'BaseLineIndex',
    component: BaseLineIndex
  }
]

export default {
  children,
  roots
}
